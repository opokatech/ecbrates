#pragma once

#include <string>
#include <vector>

namespace MO
{
    using Symbol  = std::string;
    using Symbols = std::vector<Symbol>;

    static const Symbol DEFAULT_CURRENCY{"EUR"};
} // namespace MO
